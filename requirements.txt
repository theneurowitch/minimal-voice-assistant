faster-whisper
sounddevice
numpy

# --- optional: real wakeword detection (see the README section) ---
# To enable, remove the # from the two lines below and run
# `pip install -r requirements.txt` again.
# livekit-wakeword
# note: newer onnxruntime versions (1.27+) silently break the wakeword
# models. They load fine but score everything near zero. Keep this pin!
# onnxruntime>=1.17,<1.23

# --- optional: give it a voice (see the README section) ---
# To enable, remove the # from the line below and run
# `pip install -r requirements.txt` again.
# piper-tts
